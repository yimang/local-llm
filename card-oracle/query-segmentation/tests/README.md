# Planned regression coverage

Cover required query blocks, grader scales, subject-disjoint splits, deterministic
generation, Unicode offsets, and malformed inputs. Model work additionally needs
subword alignment, BIO decoding, a tiny overfit check, and save/reload equivalence.
